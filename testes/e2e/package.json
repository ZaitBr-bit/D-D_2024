{
  "name": "fichas-de-nimb-e2e",
  "private": true,
  "type": "module",
  "description": "Testes e2e: regras do livro (regras/) e Service Worker (offline.spec.mjs). Vive isolado em testes/e2e/ para que a aplicacao em site/ continue sem nenhuma dependencia de Node. A suite de paridade contra o repo pre-refatoracao foi aposentada em 2026-08-23; ver README.",
  "scripts": {
    "test": "playwright test --config=regras/playwright.config.mjs",
    "test:offline": "playwright test --config=playwright.config.mjs",
    "test:esm": "node checar_esm.mjs",
    "instalar": "npm install && npx playwright install chromium",
    "test:regras": "npm run test:regras:unidade && npm run test:regras:e2e",
    "test:regras:unidade": "node --test \"../regras/unidade/*.test.mjs\"",
    "test:regras:e2e": "playwright test --config=regras/playwright.config.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
